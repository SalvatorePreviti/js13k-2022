import { gl } from "../gl";

export const positionsBuffer = gl.createBuffer();

export const normalsBuffer = gl.createBuffer();

export const colorsBuffer = gl.createBuffer();

export const indexBuffer = gl.createBuffer();
